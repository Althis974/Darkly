First, when we check cookies generated by the site, we can see a cookie called:
"I_am_admin".

Then, in its information, in content, the value means "false" when reverse with md5.

Finally, just edit the cookie content by replacing "false" by "true" with md5 hash.
Last but not least, refresh.
And get the flag.

# RESOLUTION

Stock valid cookies value in server.

